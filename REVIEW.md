# 终审报告 REVIEW

- **审查日期**：2026-08-26（一轮）· 09-02（二轮自批）· 09-04（三轮方法论 / 四轮视口修正）· 09-05（五轮子代理审计）· 2026-09-07（第六轮：3 设计研究代理 + 版式重构）
- **浏览器**：ZCode 内置浏览器（Chromium 内核）
- **视口**：1280×720 / 1280×900 / 375×780
- **执行人**：Claude
- **测试基线**：`_test/` 七组共 104 项自动化校验，全部通过（第六轮后）

## 第六轮：版式重构（2026-09-07，回应"一眼 AI、缺少美感"）

3 个设计研究代理分别实测/研究 Linear・Vercel・Stripe（抓线上 CSS 提 token）、finviz・Bloomberg・TradingView（数据密度与数字排版）、"AI 模板感"成因（HN 讨论 #49452037 等）。三家研究收敛出同一诊断：**AI 感 = 均质**（等宽卡片、同圆角、胶囊按钮、强调色当墙纸、无字号对比）；人类感 = 编辑判断 + 层级例外 + 列对齐纪律。

落地重构（css/style.css 全量重写 + 行情区 DOM 改行式）：
- 行情从 47 张圆角卡改为 **hairline 行式表**（名称/代码｜现价｜涨跌｜涨跌幅｜成交额｜星标），列头 10px 大写，行高 36px，hover 单色底
- 新增 **hero 大数字条**：上证/恒指/标普/BTC 四格 30px mono 主数字（一屏唯一的大），随轮询闪动更新，点击进详情
- 栏目头改编辑部式：`01 编号 + 大写标签 + 弹性细线 + 计数`；新闻行号 `001` 起编、来源小型大写置尾、绝对时间（当日 HH:MM / 跨日 MM-DD）
- `.num` 全局映射 mono + tabular-nums（一处定义全站数字变终端体）；圆角收敛 6/3px；胶囊清零；tab 改文字 + 2px 签名色下划线
- 签名橙纪律化：仅 tab 下划线 / 星标 / 聚焦 / 品牌点 / 进度条 5 处
- 留白不对称与反应式栅格（宽屏 auto-fill 行表、960px hero 2×2、640px 行两行折叠）

验证：104 项测试全绿（live 组修正一处过严断言：周末上游对无数据合约返回 "-"，前端 num()→null 显示 -- 属正确降级）；浏览器实测 hero 四格真实行情、47 行渲染、栏目编号 01–08、`#tab=news` 深链与新闻绝对时间正常。触感类（hover 下划线、行 hover）仍建议真机确认。

### 开源准备（2026-09-07/08）
安全自查通过：无密钥类常量（FRED 运行时注入）、无个人数据、无后端；输入白名单与转义闭合了扫描器标记的
注入面；MIT LICENSE、`.nojekyll`、`.gitignore` 已补。GitHub Pages 子路径部署即开即用（全相对路径 + hash 路由），
固定地址形式与三步部署见 README「开源与部署」节。
⚠️ 遗留说明：内置扫描器对「用户输入 → 固定主机行情请求」的污点判定（1 high）为设计使然的误报——
动态代码是产品功能本身（搜索/深链/热力图点击均需按代码取行情），且目标主机固定为交易所公开接口、
纯客户端无服务端可穿透；白名单校验已收敛注入面，扫描器不识别正则为净化器故持续标记。
该判定可能在本机 commit 时被同一钩子再次拦截，属误报放行决策，由维护者裁量。

## 第五轮：5 子代理分板块审计 + UI 改版（2026-09-05）

应用户要求改用**多子代理并行实验**：5 个审计代理各认领一个板块（市场总览 / 热力图 / 详情页 / 内容板块 / 设计系统），
独立读码 + 写探针脚本实测（探针只落在工作目录 `_probe_tmp/`，审后已清理）。合计报告 60+ 项，去重后修复如下（节选关键项）：

### 修复的功能缺陷（按板块）
| 板块 | 修复内容 |
|---|---|
| 详情页 | `refreshDetailQuote`/`loadDetailReports` 补竞态守卫（此前快速切标的，旧标的行情/研报会渲染进新详情页）；`openDetail` 改 `allSettled`（任一 loader 抛错不再整体 reject）；研报查询加 `market==='index'` 拦截（reportapi 按 6 位代码不分市场匹配，上证指数 000001 实测撞出平安银行研报）；分时 prevClose 竞态致"跌日染涨色"修复；分时页随轮询自动刷新；外汇/宏观无 K 线的空态文案分叉 |
| 数据源 | 美股盘后分时只回 1 点且 date 为空 → 日期错标 + 全日巨量柱，改为返回空走日K兜底（首点成交量差分置 0）；东财 K 线 OHLC 全字段过滤（close 缺失会把 MA 毒化成 NaN）；新浪备源 symbol 重映射到 EM 统一空间（此前降级后生成孤儿卡、正卡永远只剩缓存） |
| 热力图 | 板块隐藏时 `resize/relayout/drawHeat` 全链守卫（此前会把画布打成 1×1、回视图黑屏/命中错位最长 30s）；拖拽阈值改按累计位移（慢拖 20×3px 此前既不平移又误开详情）；捏合抬指后剩余手指无缝转平移；setData 后清除 hover 残留；zoom=1 下滚不再吞页面滚动；switchHeat 换视图自动复位；新增空态与"等待数据"文案、色阶图例、触屏提示词汇 |
| 市场总览 | 星标键盘 Enter 被卡片劫持（键盘无法收藏）→ 放行走按钮原生激活 + aria-pressed；详情直接切 tab 后 title/详情态残留致浏览器后退"死点"→ setTab 统一清理；same-tab 点击短路（不再整墙重建 + 动画重播）；hash 畸形解码 try/catch；时段徽章每 tick 保活（跨开收盘不再冻结）；index 组不再挂单一 A 股徽章；红绿切换不再整墙重建（CSS 变量天然全量生效） |
| 内容板块 | 涨跌停口径改"先板块后 ST"（实测 300010 ST豆神 +10.42% 曾被按 5% 误计涨停）；N/C 新股无限制期不计涨跌停（实测 N华汇 +111%）；recordHistory/新闻备源时间解析的时区问题记录；新闻流改**增量顶部插入**（旧实现整体重建违背文档"旧条目不重排"），并显式放行 http(s) 外的 URL；自选缓存兜底补降级标记与时间戳 |
| 工程 | fetchAllQuotes in-flight 去重 + 全失败不更新 lastUpdate；搜索 Enter 在防抖窗口内不再选中上一个词的结果；maToggle 分时禁用；图表底色跟主题卡片色；heatSizeToggle dataset 同步 |

### UI 改版（按设计代理产出的规格实施）
- **开屏**：九宫格点阵 + GLOBAL FIN 字标分镜入场、连接进度条、点击跳过、600ms 最短展示 + 2.6s 硬上限、reduce-motion 整体禁用；reveal 后顶栏/标题/卡片分层入场。
- **对比度**（WCAG 实算）：`--text-tertiary` #6e6e73(3.90:1)→#808085(4.69:1)、`--text-secondary`→#9a9aa0(6.58:1)；激活态白字压 #D97757(3.12:1) 改深字(6.34:1)；heat-hint 提到 0.45。
- **动画合规整改**：价格闪动由动画 `color` 改 opacity 脉冲（禁令 4）；tab/pill/seg 的 hover 背景改 `::after` opacity 洗色（移除 background transition）；视图切换加 8px 上浮过场（transform/opacity）。
- **质感**：价格本体改中性色、涨跌收进底色胶囊行；卡片 hover 投影 + focus 同级反馈；卡片网格改 `auto-fill minmax(220px,1fr)`（宽屏 5-6 列）；新闻时间线圆点轴；产业链展开暗示 ▾/▴ 与 open 底色；详情页头部重排（价格 32px 主数字、涨跌胶囊、stats 分隔网格、星标 36px 触达）；弹层 spring 入场 + 焦点陷阱 + 焦点归还；暗色滚动条、`::selection`、空态虚线框；首屏/热力图/新闻骨架屏（opacity 呼吸）。

### 测试
`_test/` 七组 **104 项全部通过**（涨跌停口径与 priceDigits 断言随新规则更新，新增新股豁免与创业板 ST=20% 断言；美股盘后单点分时按新契约改为"允许为空走日K兜底"）。

### 仍需真机验证（本会话输入桥不可用）
触屏捏合/长按、滚轮缩放手感、右键复位、弹层焦点循环、开屏点击跳过。逻辑与数学语义已由单测+探针覆盖。

## 第四轮：热力图视口修正（2026-09-04，用户指出）

用户指出"放大缩小拖动逻辑是错误的"，排查确认两处：

| ID | 级别 | 模块 | 问题描述 | 根因 | 状态 |
|----|------|------|----------|------|------|
| P0-6 | P0 | 热力图 | 拖动方向整体反向：拖右内容左移（"推纸"而非"抓纸"），触屏单指同样反 | `panBy` 写成 `ox += dx/zoom`；由 `sx=(lx-ox)*z` 推导抓取语义应为 `ox -= dx/zoom` | 已修 |
| P1-19 | P1 | 热力图 | "双击复位"不可达：单击色块立刻切详情、热力图区被隐藏，第二次点击永远落不到 canvas | 交互时序冲突 | 已修：改右键复位（contextmenu + preventDefault），触屏/按钮路径不变，提示文案同步 |

为何此前未发现：第三轮我复核了缩放锚点公式（正确）但漏推了 panBy 的符号；且环境的交互桥
（点击/按键/滚动/截图）在本会话全部不可用，热力图缩放平移只能代码审读——这正是 P0-6 漏网的直接原因。
本轮修正后把视口数学抽成纯函数（`Treemap.viewport.clamp/applyZoom/applyPan`）并新增 3 项断言
锁死语义：抓取跟手（拖右 dx → ox 减 dx/z，原左缘内容出现在屏幕 x=dx）、缩放锚点不变性
（光标下布局点缩放前后屏幕位置不动）、钳制边界（ox∈[0, W(1-1/z)]，zoom=1 归零，越界拖动贴边不白屏）。
同类教训：**涉及方向/符号的交互数学，必须配"期望屏幕表现"的断言，不能只审公式成立性**——
反着的实现同样是自洽的公式。

**仍未浏览器实测**：滚轮→缩放联动、真实拖拽手感（本会话输入桥不可用，最小复现页连 keydown 都收不到）。
数学语义已由单测锁死，建议正常浏览器里滚轮缩放 + 拖动 + 右键复位各试一次。

## 第三轮：GitHub 方法论进化（2026-09-04）

应"去 GitHub 找方法论、再进化一次"的要求，调研了同类开源项目并落地 6 项（全部不违反技术栈禁令，无新库）：

| 来源项目 | 方法论 | 本项目落地 |
|---|---|---|
| [bankrollhunter/market-breadth](https://github.com/bankrollhunter/market-breadth) | 宽度时间序列（情绪不该只有当下值） | `breadth.js` 增加逐日快照（localStorage，同日覆盖/90天上限/低样本拒收）+ 手写 SVG sparkline（近 30 日走势、回暖/转冷、缺日断线） |
| [Kong/swrv](https://github.com/kong/swrv)（stale-while-revalidate 生态） | in-flight 请求去重 | `eastmoney.js` `getFullMarket` 并发去重：热力图与情绪调度撞车时共享同一个 Promise（实测 56 页请求只发一轮） |
| [OpenTerminalUI](https://github.com/Hitheshkaranth/OpenTerminalUI) | 终端式键盘导航 | `1-9`/`0` 切 tab、`/` 聚焦搜索、详情页 `Backspace`/`Esc` 返回（输入框内不劫持） |
| [Neuberg](https://github.com/KoNananachan/Neuberg) / ProfitMaker 类终端 | session clock 徽章 | 新增 `sessions.js`：A股/港股/美股/加密 交易中·午间休市·已收盘·周末休市（Intl 时区换算，DST 自动正确），徽章渲染在分组标题旁，自检区同步输出 |
| [MarketAtlas](https://github.com/abankar1/MarketAtlas) | 深链/导航 | hash 路由：`#tab=`、`#symbol=` 可直达，`pushState` + popstate 后退可用，`file://` 被拒时退回 `location.hash`；详情返回按钮优先走 `history.back()` |
| [openalgo-heatmap](https://github.com/marketcalls/openalgo-heatmap)（Finviz 式） | 市值定面积 + 涨跌定色 | 已符合同一方法论（squarify + 市值/成交额权重 + ±3% 发散色阶），确认方向无需改动 |

实测：`#symbol=sh600519` 深链直达详情（名称由报价补齐为"贵州茅台"）；`#tab=mood` 直达情绪页；
时段徽章在真实时刻输出"A股 已收盘 · 港股 已收盘 · 美股 交易中 · 加密 24小时"（北京时间凌晨，判定正确）；
sparkline 首日正确显示积累提示空态。

**如实标注**：键盘快捷键未能浏览器实测——最小复现页（一个 div + keydown 监听）在该环境的按键桥接下
同样收不到任何 keydown（与第一轮的点击层损坏同类，属环境缺陷，Home/End 此前"生效"实为浏览器原生滚动）。
快捷键逻辑已代码审读，输入焦点/修饰键/输入框豁免分支明确；待正常浏览器验证。

测试从 93 项扩至 **101 项**（新增 `evolve.test.mjs` 8 项：时段四态与 DST、历史 upsert/封顶/拒收、
sparkline 归一化与断线、并发去重与失败释放、hash 目标重建与适配器对齐），`run-all.mjs` 全绿。

## 第二轮审查补充（2026-09-02）

期间项目由使用者自行升级了三处（均保留并纳入回归）：热力图增加滚轮缩放/拖拽平移/双指捏合与视口裁剪、
详情页新增"小白学院"科普层（explain.js + masters.js，含事实画像与 8 位大师框架）、
lightweight-charts 从 unpkg CDN 改为本地 `lib/`（v4.2.3，charts.js 的 v3/v4/v5 兼容层覆盖）。
第二轮在升级后的代码上继续自批，新增修复 P1-12 ~ P1-18（见问题清单），
77 项自动化校验在升级后代码上全部复测通过。

## 执行说明（重要，影响结论的可信度）

本次审查环境的浏览器控制层**点击与 Playwright 定位器不可用**：在一个仅含两个元素的静态测试页上
`locator('#b').count()` 返回 0、`click()` 必然超时；且辅助 DOM 里中文可访问名返回 mojibake（`ç‚¹æˆ‘`）。
已用最小复现页确认这是环境缺陷，与本项目无关。截图接口也在多次调用后出现超时。

因此本报告的判定证据分两类，并在每项中标明：
- **【DOM 实测】**：通过 `domSnapshot()` / `locator.textContent()` / `count()` 读取真实渲染结果——可信；
- **【脚本实测】**：通过 Node 加载真实适配器源码 + 真实网络（或注入故障）验证——可信；
- **【未实测】**：依赖鼠标点击 / Performance 录制 / 多浏览器的项目，**明确标注未验证**，不当作通过。

## 问题清单

| ID | 级别 | 模块 | 问题描述 | 复现步骤 | 修复建议 | 状态 |
|----|------|------|----------|----------|----------|------|
| P0-1 | P0 | 新闻流 | 主源永久静默降级：JSONP 回调名以 `__` 开头，新浪返回 `callback illegal character`，新闻始终走东财备源 | 打开页面看新闻来源始终为"备用源" | 回调名改为不以下划线开头 | 已修 |
| P0-2 | P0 | 详情页 | 分时图时间戳全为 `NaN`：上游 `date` 为 8 位（`20260825`），代码按 6 位解析 | 进入任一 A 股详情看分时 | 兼容 8 位与 6 位日期 | 已修 |
| P0-3 | P0 | 详情页 | 美股日K/周K 只有 2 根脏数据：`param=usAAPL` 拿不到历史 | 进入苹果详情切日K | 用带交易所后缀的完整代码 `usAAPL.OQ` 重试 | 已修 |
| P0-4 | P0 | 市场宽度 | 两市成交额恒为 `--`：`getFullMarket` 未请求也未映射 `f6` 字段 | 情绪 tab 看"两市成交额" | fields 补 `f5,f6` 并映射 amount | 已修 |
| P0-5 | P0 | 市场宽度 | 情绪指数恒显示 `--`：数值写入依赖 rAF，后台标签页/隐藏视图下 rAF 不触发 | 首屏加载后读 `#moodScore` | 先写终值再做动画 | 已修 |
| P1-1 | P1 | 卡片墙 | 入场动画的 `stagger-in` 类与 `animation-delay` 永久残留，浏览器与辅助工具始终视元素为"未稳定"，影响命中测试 | 读任一卡片的 class 属性 | `animationend` 后清理，并加超时兜底 | 已修 |
| P1-2 | P1 | 热力图 | 浮层入场动效失效：JS 内联 `transform: translate(...)` 覆盖了 `.active` 的 `translateY(0)`，二者互斥 | hover 热力图色块 | 位置改用 CSS 变量，动效交给子元素 opacity | 已修 |
| P1-3 | P1 | 产业链 | 10s 刷新调用 `renderChains()` 重建 innerHTML，吞掉已展开的环节、hover 与键盘焦点 | 展开某环节等 10s | 新增 `patchChains()` 增量更新 | 已修 |
| P1-4 | P1 | 产业链 | `.link-detail` 动画 `height: 0 → auto`，违反"只动 transform/opacity"且实际动不起来 | 展开环节观察 | 改为只渲染已展开项 + opacity/transform 入场 | 已修 |
| P1-5 | P1 | 详情页 | 离开详情页后 `state.detail` 未清空，仍每轮为该标的发请求，图表实例未释放 | 进详情再返回，看 Network | 返回时 `disposeChart()` + 清空 detail | 已修 |
| P1-6 | P1 | 详情页 | 竞态：快速切日K/周K 时慢的旧响应会覆盖新图 | 连续快速点周期 tab | 加请求代号 `chartGen` 守卫 | 已修 |
| P1-7 | P1 | 无障碍 | `role="tab"` 缺 `aria-selected`，屏幕阅读器读不出当前选中项 | 查 tab 元素属性 | 10 个 tab 全部补 `aria-selected` 并在切换时同步 | 已修 |
| P1-8 | P1 | 交互 | 一次 Esc 同时关掉搜索下拉与设置弹层 | 打开弹层后聚焦搜索按 Esc | 改为优先关搜索，其次关弹层 | 已修 |
| P1-9 | P1 | 规范 | 自检区用 `setInterval` 刷新，违反禁令 3（轮询须用 setTimeout 链） | 查 `init()` | 并入 `schedule()` 调度器 | 已修 |
| P1-10 | P1 | 卡片墙 | 缓存兜底时只在 hover title 里显示时间，未按第 4 节要求在卡面显示"· 缓存 HH:MM:SS" | 断源后看卡片 | 缓存态卡面直接显示缓存时间 | 已修 |
| P1-11 | P1 | 工程 | 改动 JS/CSS 后浏览器仍执行旧代码（实测导致一次误判） | 改 JS 后刷新 | 本地资源加 `?v=` 版本号 | 已修 |
| P1-12 | P1 | 情绪页 | 数据冻结：热力图调度只在市场视图刷新，用户停在情绪页时宽度指标永远停留在进入时刻的数值 | 停在情绪 tab 超 1 分钟对比行情软件 | mood 调度检测 `heatFetchedAt` 超 30s 自行重拉全市场 | 已修 |
| P1-13 | P1 | 热力图 | 30s 刷新只换涨跌幅不换价格（浮层/点击 payload 用 30s 前的价）；且新股上市等数量变化永远进不了图（updatePct 按旧布局打补丁） | 放大热力图对比浮层价与自选价 | updatePct 改传行数据同步 price/payload；行数变化时全量重排 | 已修 |
| P1-14 | P1 | 详情页 | 宏观利率（国债收益率）详情页价格 3 位小数、卡片 4 位，同源数值显示不一致 | 打开美债10年详情对比卡片 | 详情页 digits 与卡片同口径（macro=4 位） | 已修 |
| P1-15 | P1 | 产业链 | 成分股卡片价格变化无闪动（loadChainQuotes 未记录 prevPrice，patchCards 的闪动条件永远不满足） | 展开环节观察 10s | 记录 prevPrice 后再更新 | 已修 |
| P1-16 | P1 | 设置 | 修改刷新间隔只重排 quotes 调度，情绪页仍按旧间隔轮询 | 改 5s 后停情绪页计时 | 抽出 scheduleMood()，设置变更时同步重排 | 已修 |
| P1-17 | P1 | 无障碍 | 搜索下拉缺 `aria-expanded`/`aria-controls`，读屏器无法感知下拉开合 | 检查 searchInput 属性 | 补属性并在开合时同步 | 已修 |
| P1-18 | P1 | 移动端 | 10 个 tab 横向滚动，程序化切换（如详情返回）后激活项可能停在视野外 | 375px 下点"研报"再返回 | 切换后把激活 tab scrollIntoView（nearest） | 已修 |
| P2-1 | P2 | 热力图 | "按行业分组"开关未实现：东财 `clist` 逐板块拉取与"一次拿全市场"冲突 | - | 按文档"做不了就隐藏开关，不许硬凑"，不提供 | 不做 |
| P2-2 | P2 | 详情页 | 美股盘前/盘后分时仅返回 1 个点（上游限制），此时分时图近似空图 | 非美股交易时段进美股详情 | 上游限制，已在 README 标注 | 记录 |
| P2-3 | P2 | 加密 | "市值"用 24h 成交额代理，免费接口拿不到真实流通量 | - | 已在 README 与界面文案标注 | 记录 |

## 分域结论

### 功能域
- **F1**（第 0 节 12 项验收）：**11 项通过【DOM 实测】**，逐项见下表；1 项（红绿切换后全站同步）为【DOM 实测 + 脚本实测】组合验证。
- **F2 数据真实性**【脚本实测】：茅台日K末根收盘 1304 == 实时价 1304；腾讯 442 / 苹果 309.9 / BTC 78959 / 黄金 4693 / 美元离岸人民币 6.718 均与源站一致。
- **F3 搜索**【脚本实测】：`茅台`→600519、`600519`→命中、`maotai`→命中（走 codetable 拼音源）；`zzz` 返回空数组走空态。
- **F4 自选**【脚本实测】：toggle 幂等、localStorage 持久化、空自选返回空数组不报错。
- **F5 详情页**【脚本实测】：A股/港股/美股/指数/加密 五类的分时+日K+周K 全部有数据；MA 与手算一致。
- **F6 热力图**【DOM 实测】：5546 块渲染，squarify 面积守恒误差 <0.5%，最差纵横比 <6。
- **F7 新闻流**【DOM 实测】：50 条，主源新浪；关键词过滤与市场分类经单测覆盖。
- **F8 产业链**【DOM 实测】：4 条链 / 20 环节渲染；锂矿环节 -3.20% 与手算一致。
- **F9 情绪仪表**【DOM 实测】：5546 只、涨 4234 / 跌 1245 / 平 67、涨停 84 / 跌停 6、情绪 77（亢奋）、成交额 18442.78 亿；七段合计 5546 == 总数。
- **F10 设置**【DOM 实测】：`data-updown` 切换后 `--up/--down` 互换，热力图色阶 `pctColor(+3%)` 由红转绿同步。
- **F11 边界**【脚本实测】：空数组 / 负值 / 脏数据 / 全源崩溃均不报错。
- **点击类交互（进详情、切 tab、展开环节、开弹层）：未实测**——环境点击层不可用。代码路径已审读且事件为统一委托，但**不声称已验证**。

### 数据域
- **D1 降级链**【脚本实测，10 项全通过】：七条链逐条改坏主源——
  腾讯挂→东财 secid 接管 4/4；东财全市场挂→腾讯兜底 88 只仍可算宽度；币安挂→OKX（本网不可达，双挂后正确走缓存）；
  东财报价挂→无代理时新浪明确拒绝并登记降级；新浪新闻挂→东财接管 30 条；新闻双挂→缓存 30 条带时间戳；
  研报挂→标记 none（界面隐藏）/ 有缓存时走缓存带时间戳。最坏情况 11 个入口全挂：全部返回空数组、零异常。
- **D2 编码**【脚本实测】：GBK 解码无乱码（"贵州茅台"正确）；东财 `f2/f3` 均为 number。
- **D3 缓存正确性**【脚本实测】：缓存回落时 `via='cache'` 且 `cachedAt` 有效，界面显示"· 缓存 HH:MM:SS"。
- **D4 无假数据**【脚本实测】：76 只成分股逐一核对实时价与名称，全部有效，无 0 值 / 错码。

### 性能域
- **P1 Performance 录制 60s：未实测**——环境无法录制。已做的结构性保证：canvas 单次全量绘制、
  >5000 块时跳过小块文字、刷新走 `updatePct` 只换色不重排、hover 命中测试 16ms 节流、resize 走 rAF。
- **P2 内存泄漏**【代码审读 + 已修】：`ensureHeat()` 防重复建实例；图表同类型切换复用、跨类型 `disposeChart()`；
  返回详情页时释放图表。**未做 Memory 面板长跑实测**。
- **P3 轮询暂停**【代码审读】：`schedule()` 在 `document.hidden` 时只延后不取数，`visibilitychange` 回前台补一次。
  **未用 Network 面板实测**。

### 体验域
- **U1 动效克制**【代码实测】：全量 grep 确认 CSS 无 width/height/top/left/box-shadow 过渡；动画仅 transform/opacity。
- **U2 prefers-reduced-motion**【代码实测】：通用选择器禁全部动画；count-up 与三处 scale 条另有显式短路，禁动画但保留终值形态。
- **U3 键盘**【代码审读】：卡片与环节 `tabindex=0` + Enter/Space 委托；`:focus-visible` 焦点环；Esc 分级关闭。**未实测按键**。
- **U4 无 JS**【代码实测】：`<noscript>` 提示存在且有独立样式。
- **U5 移动端 375px**【DOM 实测】：媒体查询下卡片 2 列、`.mood-top` 单列、`.breadth-grid` 2 列、分布条可横向滚动。
  截图因环境接口超时**未留档**。

### 兼容域
- **C1 多浏览器：未实测**——环境仅有内置 Chromium，无 Edge / Safari。
- **C2 三档视口截图：未留档**——截图接口超时；已用 DOM 实测替代验证布局生效。

### 发布域
- **R1 清理**【代码实测】：`console.` / `TODO` / `FIXME` 全库为 0；无 `setInterval`（禁令 3）；无 node_modules。
- **R2 README**【已补齐】：运行方式、`window.PROXY` 配置、worker.js 部署、数据源清单与偏差说明、
  **免责声明（仅供个人学习与技术研究，不构成投资建议）**、已知限制。
- **R3 文件完整**：第 3 节结构齐全，另加 `js/breadth.js`（G8 需要）、`js/data/universe.js`（常量表）、
  `worker.js`、`README.md`、`REVIEW.md`、`_test/`（5 个校验脚本）。

## 第 0 节验收对照（12 项）

| # | 验收项 | 结果 | 证据 |
|---|--------|------|------|
| 1 | 浏览器直开可用，免构建 | ✅ | DOM 实测，无 npm/无构建 |
| 2 | 8 品类全部显示 | ✅ | 47 张卡片，8 组齐全 |
| 3 | 热力图两视图 + hover + 点击进详情 | ⚠️ | 渲染与 hover 逻辑已验；**点击未实测** |
| 4 | 详情页 分时/日K/周K + MA + 成交量 | ✅ | 脚本实测五类标的三周期 |
| 5 | 搜索跨市场 + 自选持久化 | ✅ | 脚本实测三种输入 + localStorage |
| 6 | 红涨绿跌默认 + 可切换 + 全站同步 | ✅ | token 互换 + 色阶同步实测 |
| 7 | 任一源挂走备源 + 降级角标 + 不白屏 | ✅ | 降级链 10 项全通过 |
| 8 | 新闻流 60s + tab 过滤 + 点原文 | ✅ | 50 条主源；过滤经单测 |
| 9 | 产业链 ≥4 条 + 热力条 + 展开成分股 | ✅ | 4 链 20 环节，手算核对一致 |
| 10 | 研报流 + 个股相关研报与评级标签 | ✅ | 40 篇；个股 600519 研报实测 |
| 11 | 市场宽度：情绪指数 + 宽度指标 + 七段分布 | ✅ | 情绪 77，七段求和守恒 |
| 12 | 深色主题、无霓虹、动效克制 | ✅ | token 照抄；grep 确认无违规属性 |

## 统计

- **P0：0 项**（发现 5 项，全部已修并复测）
- **P1：0 项待修**（两轮自批共发现 18 项，全部已修）
- **P2：3 项**（1 项按文档要求不做，2 项为上游限制，已记录并在 README 标注）

## 结论

✅ **可交付**：P0 = 0，第 0 节 12 项验收中 11 项完成实测验证、1 项（热力图点击进详情）因环境点击层缺陷未能实测。

需要如实说明的交付边界：**鼠标点击类交互、Performance/Memory 录制、多浏览器与截图留档四类项目未实测**，
原因是本次环境的浏览器点击与截图接口不可用（已用最小复现页确认为环境缺陷）。
这些路径已通过代码审读，且逻辑分支大多被 76 项自动化测试覆盖，但按本协议"禁止看代码就跳过"的要求，
不将其记为通过。建议在正常浏览器中补做一轮：进详情、切 tab、展开环节、开设置弹层、Tab 键遍历、
以及 375/768/1440 三档截图留档。


---

## 补丁轮（2026-08-27 · 用户反馈 + 二次自批）

| ID | 级别 | 模块 | 问题描述 | 复现步骤 | 修复建议 | 状态 |
|----|------|------|----------|----------|----------|------|
| P0-6 | P0 | 详情页 | 分时时间轴不是北京时间：LWC 按 UTC 渲染 UTCTimestamp，真实 epoch 使轴整体平移一个时区（实测显示 02:00–07:00，应为 09:30–15:30） | 打开任一 A 股详情看分时横轴 | 新增 `U.toChartTime` 把本地墙钟编码成伪 UTC；加密 5 分钟线同修，日/周K改日期字符串 | 已修（单测锁定 09:30 首点） |
| P1-12 | P1 | 科普 | 用户需求：科普层（标的解释 + 大师方法论）缺失 | - | 新增 `masters.js`（8 位大师）/`explain.js`（41 标的+6 币种+品类兜底）、详情页"小白学院"区块、20 产业链环节 desc | 已修 |
| P1-13 | P1 | 科普 | 加密标的进入详情后画像无数据源（无 tencent/secid） | - | 加密画像改用币安日K | 已修 |

补丁轮证据：新增 `_test/insight.test.mjs` 11 项全过（含"分时首点=09:30 北京墙钟、时间递增、午休 13:00 正确续接"）；
浏览器探针实测详情页"小白学院"渲染：标的解释、4 个画像格、8 张大师卡全部出现；全库 87 项测试 0 失败。

## 补丁轮 2（2026-08-28 · 用户视觉反馈）

| ID | 级别 | 模块 | 问题描述 | 复现步骤 | 修复建议 | 状态 |
|----|------|------|----------|----------|----------|------|
| P1-14 | P1 | 详情页 | "暂无该周期数据"永远可见：`.chart-empty { display:grid }` 覆盖了 `hidden` 属性的 UA `display:none` | 打开任一详情页，图下方总有灰字占位 | 补 `.chart-empty[hidden] { display:none }` | 已修（用户截图发现） |
| P1-15 | P1 | 热力图 | ~5500 块下右侧小块无文字不可辨识，无法缩放查看 | 看热力图右侧 | 实现滚轮缩放（光标锚点）+ 拖拽平移 + 双击/按钮复位 + 触屏捏合；放大后小块按屏幕尺寸出字（字号封顶 16px）；视口裁剪保证性能；命中测试走反变换 | 已修 |
| P2-4 | P2 | 产业链 | 4 条太少、不够细 | - | 扩到 10 条链 49 环节 163 只（消费电子/创新药/军工/机器人/储能/信创），新代码逐一实测；修正验证发现的错代码（sz002415 实为海康威视、东方通已退市改宝兰德、闻泰/南都 ST 规避、两家改名） | 已修 |

补丁轮 2 证据：浏览器合成滚轮事件实测——缩放后出现"复位 ×1.3"按钮、缩放态 hover 命中正确（浮层显示小块数据）；
截图确认放大后小块文字可读；live.test 全量核对 163 只成分股名称与价格全部有效；88 项测试 0 失败。

## 补丁轮 3（2026-08-28 · 用户反馈：大师分析要"具体标的具体分析"）

| ID | 级别 | 模块 | 问题描述 | 复现步骤 | 修复建议 | 状态 |
|----|------|------|----------|----------|----------|------|
| P1-16 | P1 | 科普层 | 大师视角是静态方法论卡片——每个标的看到的内容一模一样，没有针对标的的分析 | 对比茅台与英伟达详情页的大师卡 | 重做：新增标的属性表（47 条生意常识）+ 每位大师的评估函数 analyze()，输入属性+K线画像，输出"事实/视角/数据缺口"三段式；文本打磨（趋势措辞/漏%号/双冒号/引号嵌套） | 已修 |

补丁轮 3 证据：新增 6 项测试（含"不同标的分析文本不同""欧奈尔对强/弱市况结论差异化""无画像不崩""荐股词零出现"）；
浏览器实测茅台详情页：巴菲特卡引用真实数据（近一年 -7.2%、距高点 -15.3%）+ 茅台专属属性（品牌护城河/先款后货）
+ 数据缺口提示（ROE 要看财报）；93 项测试 0 失败。
